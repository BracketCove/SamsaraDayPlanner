<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
    </style>

    <style name="OutlineTextBox" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@drawable/input_box_style</item>
        <item name="hintTextColor">@drawable/input_box_style</item>
        <item name="android:layout_margin">16dp</item>
    </style>

    <style name="ColorSheetButtons" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_margin">16dp</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>

    </style>

</resources>
